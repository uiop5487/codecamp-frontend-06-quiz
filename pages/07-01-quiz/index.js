import { useQuery, gql, useMutation } from "@apollo/client";
import MapProductdContainer from "../../src/componets/unit/list/list.container";

const MapProductdPage = () => {
    return <MapProductdContainer />;
};

export default MapProductdPage;
